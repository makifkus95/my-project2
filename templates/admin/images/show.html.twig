{% extends 'base.html.twig' %}

{% block title %}Images{% endblock %}

{% block body %}
    <h1>Images</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ image.id }}</td>
            </tr>
            <tr>
                <th>Productid</th>
                <td>{{ image.productid }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ image.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_images_index') }}">back to list</a>

    <a href="{{ path('admin_images_edit', {'id': image.id}) }}">edit</a>

    {{ include('admin/images/_delete_form.html.twig') }}
{% endblock %}
